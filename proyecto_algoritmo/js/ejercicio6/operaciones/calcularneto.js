// modulo 3: Salario Neto = base - deducciones
export function calcularneto(salarioBase, deducciones) {
    // el calculo neta se realiza a traves del salario base - las deducciones
    // en el cual retonramos el salario base del trabajdor y le restamos las deducciones 
    return salarioBase - deducciones;
}